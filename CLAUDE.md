# CLAUDE.md — AI Assistant Guide for Apple2

## Project Overview

Apple2 is a newly initialized repository. This document establishes conventions and guidelines for AI assistants working on this codebase. Update this file as the project evolves.

## Repository Status

This repository is in its initial state with no source code yet. As code is added, update the sections below to reflect the actual project structure, build system, and workflows.

## Project Structure

```
Apple2/
├── CLAUDE.md          # This file — AI assistant guide
└── (empty)            # No source files yet
```

As the project grows, document new directories and key files here.

## Build System

No build system has been configured yet. Update this section when one is added (e.g., Makefile, CMake, Xcode project, etc.).

## Development Workflows

### Branching

- Feature branches follow the pattern: `claude/<description>-<id>`
- Push with: `git push -u origin <branch-name>`

### Testing

No test infrastructure exists yet. Update this section when tests are added.

### Linting / Formatting

No linting or formatting tools are configured yet. Update this section when they are added.

## Key Conventions

- Keep this CLAUDE.md file up to date as the project evolves
- Prefer editing existing files over creating new ones
- Write clear, descriptive commit messages
- Do not commit secrets, credentials, or environment files

## Dependencies

No dependencies have been declared yet. Update this section when a package manager or dependency file is introduced.

## Common Tasks

<!-- Add common development commands here as the project matures, e.g.: -->
<!-- - `make build` — Build the project -->
<!-- - `make test` — Run tests -->
<!-- - `make clean` — Clean build artifacts -->
